# password-gen

This is a password generating console app which creates random passwords with numbers (0-9), lowercase English letters (a-z) and uppercase letters (A-Z). It uses CSPRNG to make passwords more secure and has a punishment system for cases when the password consists of too many letters and numbers in a row to make it less predictable.
